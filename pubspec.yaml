name: quarantine
description: A house, an hour at a time — pure Dart, WebGL2, no Flutter.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  pixeldart:
    path: external/pixeldart
  web: ^0.5.0

dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.1.0
  lints: ^3.0.0
  test: ^1.25.0
